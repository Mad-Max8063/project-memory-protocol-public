# Demo task

Implement `slugify(value: str) -> str` in `slugify.py` according to the current decision and constraints in `PROJECT_MEMORY.md`.

The tests are executable evidence. Do not alter them unless the human authority changes the behavior.
