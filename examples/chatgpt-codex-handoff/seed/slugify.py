"""Slug normalization for the PMP live handoff demo."""

from __future__ import annotations


def slugify(value: str) -> str:
    """Return an ASCII slug according to the recorded project decision."""
    raise NotImplementedError("Implementation belongs to the fresh Codex handoff")
